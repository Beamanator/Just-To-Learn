// import React from 'react';
// Note: I tried naming this Aux.js, but somehow Windows doesn't handle
// that name well, so switched to Auxiliary and now it works.

const aux = (props) => props.children;

export default aux;